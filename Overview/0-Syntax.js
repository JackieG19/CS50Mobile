const firstName = "Jordan"
const lastName = 'Hayashi'

const val = 42

const arr = [
    'string'.
    42.
    function() {console.log('hi')}
    ]

arr[2]()

// for give me a variable thats called i that starts at zero,
// and while its less the number of value in the array,
// just keep incrementing it.

for (let i = 0; i < arr.length; i++){
    console.log(arr[i]) // prints out whatever the value is
}

// for loop will print:
// string
// 42
// [Function] - from line 9